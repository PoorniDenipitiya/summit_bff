"use strict";
/*const axios = require('axios');
const { categoryServiceUrl } = require('../config/config');

class CategoryService {
  async getAllCategories() {
    const response = await axios.get(`${categoryServiceUrl}/api/categories`);
    return response.data;
  }

  async getCategoryById(id) {
    const response = await axios.get(`${categoryServiceUrl}/api/categories/${id}`);
    return response.data;
  }

  async createCategory(categoryData) {
    const response = await axios.post(`${categoryServiceUrl}/api/categories`, categoryData);
    return response.data;
  }

  async deleteCategory(id) {
    await axios.delete(`${categoryServiceUrl}/api/categories/${id}`);
  }
}

module.exports = new CategoryService();
*/ 
